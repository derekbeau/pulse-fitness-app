# Current checkpoint #178 — Body concerns, guidance and safe flare handling

## Authority / predecessor

Derek explicitly renewed continuing feature-branch pushes: "Ok keep it moving. You can keep pushing to the feature branch right?" This authorizes normal (not force) pushes of scoped release commits to origin feat/activity-journal-release. It supersedes the prior #177 push hold. Parent pushed and verified #177 through a46ffface3f80e3c89ba8dd9ed93f6ceee8d579c after independent Terra PASS, including real independent-process concurrency evidence. No approval for main writes, merge, deployment, production data/access, cron/config, external accounts or unrelated changes.

Continue exact lane /Users/meridian/Projects/pulse-activity-journal-release, branch feat/activity-journal-release. This advances CURRENT checkpoint to #178, superseding earlier checkpoint stop language only. Preserve all fixed invariants in activity-journal-release.md and authoritative product proposal; see child #178 snapshot and repaired contracts/route-owner inventory. No #179+ implementation, internal reviewer agents, PR/merge/issue closure/deploy. One editor. Parent independently reviews then advances. Do not repeat full history or prior checkpoint discovery.

## Deliverable

Persist and expose canonical body concerns/capabilities, guidance with all four provenance classes, immutable state/correction history, freshness/staleness, immediate flare capture, necessary follow-up state, and explicit meaningful-change proposal/approval execution. Symptoms remain separate from management state; maintenance/irrelevance never deletes concern/history. No diagnoses, inferred healing/clearance, unsupported causality, or silent meaningful plan mutation. `resolved` requires explicit user-authorized management decision, never inferred symptom-free state.

Inspect relevant live schemas/routes first: #177 Activity transactions/idempotency/concurrency harness, workout feedback planning/precaution decisions and scheduled snapshots, existing injury/health-condition domains, shared auth and date authority. Avoid duplicate sources of truth or reusing workout-specific question identity for daily context. Existing fields/history must remain preserved, with explicit adapters/links instead of fabricated clinician provenance. Additive migrations only; no rewrite of historical migrations or unsupported legacy conversion.

Implement all #178 routes in activity-journal-contracts.md, plus minimal missing lifecycle create/read/correct/detail entry points necessary for a new authenticated subject to use capabilities, concerns, guidance and proposals without manually seeded IDs. Real registered OpenAPI, correct request/response/error schemas, bounded deterministic lists, details exposing history/source links. Agent capture mutations remain conversation-managed; reads use repository shared-auth conventions. No manual-entry UI or recorder. Explicit approval can be conveyed by authenticated user or authenticated agent RELAYING a persisted explicit user approval; it must remain distinguishable from agent suggestion and must not forge a user actor. Define and test that approval contract against existing four provenance classes. Approval cannot be inferred from mere flare report or generic agent authority. Preserve fixed requirement approving user identity and exact proposal revision/target fingerprint; if existing schema makes relay impossible, record a minimal compatible clarification rather than silently dropping the user-approval invariant.

Record a flare durably BEFORE optional follow-up or plan change. Missing optional answers cannot reject/erase the flare. Only questions material to safe use; no diagnosis generation/LLM API. Persist pending follow-up facts for later #179 consumer, not a second daily check-in implementation.

Meaningful upcoming Activity/workout changes are proposed, read back and approved explicitly. Approved execution must atomically recheck exact proposal revision, approval binding, all target revisions/dependencies/ownership, and current eligibility. Reject stale proposal/target without partial mutations. Changed proposals invalidate old approvals. Retries replay original result exactly once; server computes semantic fingerprint and scope. Competing approvals/corrections must not double-apply or overwrite history. Completed workout/session history is never rewritten by prospective proposals. Reuse existing guarded scheduled-workout mutation primitives; do not bypass snapshot/occurrence identity or flatten per-occurrence note channels. Support scoped meaningful adjustments to upcoming Activities and workouts as required; document supported typed mutations, reject unsupported ones rather than generic arbitrary JSON execution. Direct routine instruction (move my walk tomorrow) uses #177 guarded reschedule once, with history, without requiring a meaningful-change proposal. Routine authority must not become a generic approval bypass for unrelated or meaningful payloads.

## Frozen acceptance cases

- Agent captures concern/capability, supplied clinician-authored guidance vs user-relayed clinician vs user observation vs agent suggestion; source links, uncertainty, freshness and correction history survive API readback/restart. Agent suggestion never labels itself prescribed.
- Flare persists immediately with partial/unknown optional context; follow-up remains pending and no plan changes occur. Test failed subsequent proposal/follow-up does not roll back the already-recorded flare.
- Active symptoms vs monitoring/maintenance/resolved management states and irrelevant concerns remain distinct with immutable history. Invalid transitions and inferred clearance denied; explicit authorized decisions audited.
- Real meaningful proposals for upcoming Activity and scheduled workout: unapproved request cannot execute; explicit exact-revision approval executes once; changed proposal/target fails visibly; completed sessions unchanged. Multi-target failing validation leaves all targets/history/receipts untouched.
- Explicit routine walk reschedule executes once with planned and actual history retained; payload cannot smuggle meaningful changes under routine operation.
- Auth-derived actor/subject, URL/body identity, foreign concern/capability/guidance/proposal/Activity/workout links and lists reject safely/non-disclosing. Agent relay cannot invent authenticated user identity; distinguish authorization to record a user statement from permission to self-approve.
- Server-derived fingerprint and receipt scope; same semantic retry after restart, altered payload conflicts even with same client fingerprint; no failed mutation receipt.
- Genuine TWO independent API processes/DB handles on shared fictional WAL DB with barriers: duplicate flare retry, stale concern correction, competing approval/execution against one proposal and stale target. Reuse #177 harness pattern. Assert exact durable counts, immutable prior history, no losing partial state, fresh-connection integrity/foreign_key_check. Single app.inject Promise.all on singleton is insufficient.
- Local-date/instant/timezone semantics at midnight/DST; bad zones fail safely; capture timestamp does not replace occurrence time.
- Actual migration runner on fresh/populated fictional exact predecessor, unchanged legacy rows, rerun/no-op, failure rollback, account erasure and integrity. No production DB/backup access. Probe REPLACE/trigger behavior only if introducing such guards.

## Efficient execution / evidence

Use focused tests while editing and consolidated self-review. No nested agents or LLM polling. Routine public frozen-lock/build prerequisites allowed; preserve lockfile and existing toolchain unless necessary. Fixtures/servers isolated and loopback-only, stop owned resources on completion.

Capture raw first-run commands/stdout/stderr/exit/source hashes in /Users/meridian/Projects/qa-reports/pulse-activity-journal-release/checkpoint-178; preserve failures and identify superseded receipts. Final risk matrix: new registered API/persistence/authorization/approval/race/migration tests plus affected workout/Activity regressions; repo test/typecheck/lint/build as warranted for DB/API/shared mutations. Reuse valid hook receipts rather than duplicate full suites. No browser gate for backend-only work. Never retry-to-green or claim cached unrelated source proves this head. If Node/SQLite ABI setup blocks, diagnose exact runtime before changing anything.

Update source-bound status, API docs/examples and route runtime ownership inventory; #176 and #177 accepted, #178 current. Commit coherent descriptive changes, push ONLY feature branch, verify exact local/remote head and clean worktree. Return exact source/evidence/readbacks/gaps, ending 'Ready for independent review: #178'. Stop there; no #179 or final release claims.
