# Current checkpoint #177 — Activity runtime

## Authority and exact continuation

Vector accepts #176 at e32812100d73af2cd23be380ce275895c0459488 after Terra delta PASS and independent reproduction of four corrected-boundary probes. Continue in /Users/meridian/Projects/pulse-activity-journal-release on feat/activity-journal-release. Main is read-only. This file advances ONLY the CURRENT checkpoint to #177, superseding the #176 stop in activity-journal-release.md; all its cross-cutting product, safety, efficiency and final approval boundaries remain. No #178+ implementation, internal reviewer agents, PR, merge, issue closure, production access/data/migrations/deploy, config/cron or unrelated cleanup. Commit/push this branch allowed. Stop for independent Terra review after #177.

## Outcome and scope

Implement the entire child #177 acceptance from docs/planning/activity-journal-issues.json and approved product proposal. Use repaired shared contracts and docs/implementation/activity-journal-contracts.md as foundation, adapting minimally if a genuine missing runtime contract is found and recording why. Agent-managed capture only; no UI work or manual-entry forms. Activities remain separate from structured workouts.

First inventory actual legacy Activity schema/rows and migration chain, entity-link rules, shared JWT/AgentToken auth, idempotency/fingerprint conventions, date/time resolver, route registration/OpenAPI, scheduled snapshot/history, existing integration tests. Legacy apps/api/src/db/schema/activities.ts DOES exist at product base; initial parent issue inventory mistakenly said otherwise. Do not invent a greenfield table assumption. Preserve existing rows and historical migrations; no made-up provenance, time of day, clinician origin, or synthetic workout identity for legacy data. Preserve ambiguous date-only legacy records transparently; if an irreversible conversion needs a product decision, stop and report it.

Implement additive persistence/store/routes for canonical Activities, multiple goal links, assignment identity/planned day and immutable reschedule history, distinct actual execution instant/day/outcome, retrospective capture, recurrence creation/materialization and prospective revisions, immutable corrections, owned cross-record links, actor/provenance/uncertainty/freshness and durable idempotency receipts. Cover all #177 routes in the inventory: Activity list/create/detail/correct; assignment create/reschedule; execution create/correct; recurrence revision. Close missing lifecycle entry/read surfaces: a new user must be able to create/manage/retrieve goal links and create a recurrence without a manually seeded ID, using minimal coherent APIs. Document these additions and exact OpenAPI contracts rather than leaving orphan functionality. Reads must expose planned and actual independently, full revision history and source links; list pagination/date filtering must be bounded and deterministic.

Derive subject and actor from authentication. Reject spoofed subject/actor/scope, foreign links and route/body ID mismatches without information leaks; schemas do not replace database ownership checks. Server computes canonical request fingerprint from validated semantic payload and derives canonical route/operation namespace; never trust a client-supplied fingerprint or scope to bypass retry protection. Same key/same semantic request replays original result, same key/different payload conflicts, failures roll back atomically. Legitimate agent identity remains distinct from subject identity. JWT and AgentToken follow repository conventions without handler-specific split schemas.

Concurrency must be database-backed: compare-and-swap revisions in the transaction, uniqueness for receipt/occurrence/assignment identity, repeated concurrent retries execute once, stale updates visibly fail with no partial writes. Recurrence materialization retries must not duplicate assignments. Revising recurrence only affects unmaterialized occurrences on/after effective date; already assigned days retain their origin/revision until explicit reschedule. Read/resume after restart uses persisted state, not process caches. Never overwrite completed actual history when rescheduling planned work. Retrospective capture preserves meaning/source timing versus capture timing; unknown is not negative. Explicit routine rescheduling is allowed and retry-safe; meaningful body/workout plan proposals belong to #178 and are not silently executed here.

## Required durable tests / acceptance

- Real registered API + DB integration: five-minute PT captured by AgentToken; API readback after application/DB reopen; corrected grammar does not alter meaning/provenance.
- Tuesday assignment executed Thursday: linked identities, both dates queryable, reschedule preserves original history and does not rewrite actual execution.
- Recurrence create/materialize/retry/revise effective date; existing assignments unchanged; subsequent unmaterialized occurrences follow new revision; bounded date ranges and invalid recurrence fail safely.
- Multiple goals, retrospective records, all supported provenance/uncertainty fields survive persistence/correction/readback. Structured workout remains a distinct owned linked identity, no duplicate workout insertion.
- Same retry result before/after restart; altered payload under same key fails even if client reuses fingerprint; spoofed route/operation/subject/actor and URL/body ID mismatch rejected.
- Two authenticated subjects with deliberate foreign Activity/assignment/execution/workout/goal links, including reads, corrections, reschedules and list results: rejection with no disclosure or partial writes.
- Genuine two-connection or concurrent HTTP races for duplicate create/materialization and stale correction/reschedule: exactly one accepted state transition, immutable earlier revision, correct visible conflict.
- UTC/local midnight and DST spring/fall; invalid IANA timezone never unhandled throw; assigned local dates remain intent not UTC-truncated instants.
- Failed transaction leaves no orphan history, receipts, goal links or assignments; supported account erasure still works without enabling cross-user audit deletion.
- New additive migration: actual production migration runner on fresh database and populated fictional exact predecessor, existing legacy records preserved, foreign_key_check/integrity_check, rerun/no-op and atomic failure behavior. Do not mutate historical migration files. If DB guards used, test actual UPDATE/DELETE/REPLACE semantics and required recursive_triggers behavior. No production DB or backups accessed for this checkpoint; canonical-data rehearsal remains a separate approval gate before deployment.
- OpenAPI includes real routes and correct request/response/auth/error shapes; non-placeholder API fixture/readback evidence. No browser gate for backend-only slice.

## Verification and receipts

Inspect package scripts/runtime Node/native SQLite ABI first. Routine frozen-lock installs/public build prerequisites are authorized; no lockfile churn to work around setup. Keep fictional DBs/servers isolated, loopback-only. Focused tests during development, self-review then one final risk-relevant matrix. Capture first-run raw output, exit status, exact command, source/test/config/dependency identity in /Users/meridian/Projects/qa-reports/pulse-activity-journal-release/checkpoint-177, preserving failures. Use existing receipt tooling if practical, not a new framework. Do not use an LLM polling loop; use bounded tool-side waits or completion events.

For this DB/API slice final gates: focused Activity/auth/concurrency/migration tests; all required affected regression suites; pnpm test, pnpm typecheck, pnpm lint, pnpm build with honest uncached identity (or reuse matching hook execution as the full test receipt, not run it again by ritual). No duplicate root/package full suites solely for reporting. Expected old migration-count fixtures can be updated precisely without weakening assertions or historical migration files. Repair any in-scope failing acceptance; preserve and report genuine unrelated blockers rather than retry-to-green.

Update route ownership runtime status, agent API docs/examples (minimal #177 commands), docs/implementation/activity-journal-status.md with #176 accepted/#177 current and exact source-bound evidence. Commit descriptive source/tests/docs changes, push and verify clean local/remote head. Return API/readback examples, migration/concurrency results, scope deviations and gaps, ending 'Ready for independent review: #177'. Never claim downstream daily context/Journal/Calendar/UI or release complete.
