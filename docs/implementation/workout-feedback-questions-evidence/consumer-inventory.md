# Pulse #150 consumer inventory and implementation plan

Status: finalized from source before feature source edits on 2026-09-08.

## Baseline

- Worktree: `/Users/meridian/Projects/pulse-workout-feedback-questions`
- Branch: `feat/workout-feedback-questions`
- Clean starting HEAD: `a2e6f7837022b46f663c74e52def9fd6d7856a94`
- The frozen goal still records its preparation parent, `6326bd7b81219a4cf4d7b2e819e09369bf4a3946`.
  The only intervening path is the goal document itself, so the user's explicit starting HEAD is
  authoritative and introduces no source conflict.
- `AGENTS.md`, the complete frozen goal, issue #150, and draft PR #163 were read before source
  edits. No app, server, browser, Codex Desktop UI, CUA session, production database, or migration
  was launched during preparation.

## Consumer matrix

| Surface                                    | Verified source behavior                                                                                                                                                                                                                                                                       | #150 action / boundary                                                                                                                                                                                                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shared definition and provenance contracts | `packages/shared/src/schemas/feedback-provenance.ts`, `workout-templates.ts`, `scheduled-workouts.ts`, `workout-sessions.ts`, and `packages/shared/src/index.ts` define/export native responses and route shapes. Schema version 2 quarantines unsupported summaries and preserves false/zero. | Add one exported authored/frozen definition contract plus answer/list revision mutations. Keep schema version 2 and legacy response parsing additive; validate new writes against frozen definitions.                                                               |
| Shared/OpenAPI tests                       | Colocated shared schema tests and API route schemas exercise generated OpenAPI.                                                                                                                                                                                                                | Cover every bound/config/type, the three-question authoring budget, conflicts, exact response shapes, and AgentToken examples. Historical over-budget forms stay read-compatible.                                                                                   |
| Template persistence/API                   | `apps/api/src/db/schema/workout-templates.ts` and `routes/workout-templates/{store,index}.ts` persist owner-scoped templates; PATCH omission preserves unrelated fields.                                                                                                                       | Persist current question-list projection and immutable revisions. Create/update accept authored defaults; omitted PATCH preserves, explicit `[]` clears.                                                                                                            |
| Scheduled persistence/snapshot/API         | `apps/api/src/db/schema/scheduled-workouts.ts`, `routes/scheduled-workouts/{snapshot-store,store,index}.ts`, and scheduled snapshot backfill currently copy exercises only.                                                                                                                    | Copy exact template question definitions at schedule creation; support an explicit replacement override with `expectedRevision`, including `[]`, and never mutate a started session.                                                                                |
| Session persistence/API                    | `apps/api/src/db/schema/workout-sessions.ts`, `db/schema/workout-session-feedback.ts`, and `routes/workout-sessions/{store,index}.ts` persist the #149 feedback blob/audits and create/update sessions.                                                                                        | Freeze core plus applicable additional definitions for scheduled/template/ad-hoc starts; add durable draft/current answer projections and immutable response revisions without replacing the #149 blob/audits.                                                      |
| Feedback submission/provenance audit       | `db/schema/{feedback-submission-audit,feedback-provenance-audit}.ts`, `db/feedback-provenance-migration.ts`, and `db/feedback-note-remediation.ts` retain raw evidence and reject unsupported actionable summaries.                                                                            | Preserve raw historical JSON and audit rows byte-for-byte. Dynamic answers do not become readiness/recovery/technique scores. Add only new immutable evidence.                                                                                                      |
| Completed corrections                      | `PATCH /api/v1/workout-sessions/:id/corrections` is currently set-only.                                                                                                                                                                                                                        | Extend the same surface with answer corrections and `expectedRevision`; append revisions and leave sets, RIR/RPE, session timing/status, and prior answers unchanged.                                                                                               |
| Scheduled start surfaces                   | `apps/web/src/features/workouts/components/{workout-list,scheduled-workout-detail}.tsx`, `lib/scheduled-start.ts`, `hooks/use-workout-session.ts`, and `scheduled-start-surfaces.test.tsx` send `scheduledWorkoutId`.                                                                          | Both surfaces must consume one server-frozen scheduled snapshot; retry/force cannot duplicate or re-read template questions.                                                                                                                                        |
| Direct/ad-hoc start surfaces               | `template-detail.tsx`, `workout-template-detail.tsx`, repeat flow in `workout-session-detail.tsx`, and the session POST route start from a template or ad hoc name.                                                                                                                            | Direct template starts copy current defaults; ad hoc freezes core and accepts only explicit valid additions. Later template edits cannot alter an active session.                                                                                                   |
| Completion renderer/wiring                 | `session-feedback.tsx` renders six input types but hardcodes RPE, energy, and pain and matches drafts by id/type/label. `active-workout.tsx` always supplies mock fields and completes through `use-complete-session.ts`.                                                                      | Reuse the renderer with frozen definitions, remove production mock reliance and universal energy, render `post_session` only, match id+version, hydrate latest server revisions, and retain local drafts on failed/stale saves.                                     |
| Completed detail/history/comparison        | `session-detail.tsx`, `pages/workout-session-detail.tsx`, `session-summary.tsx`, and `session-comparison.tsx` display completed session data.                                                                                                                                                  | Show exact prompt/version/value/state/timing/context/provenance and revision history without collapsing skipped/unknown/false/zero or generating aggregate meaning.                                                                                                 |
| Workout API client/cache                   | `apps/web/src/features/workouts/api/workouts.ts`, `lib/session-query-cache.ts`, `hooks/use-workout-session.ts`, and `lib/query-invalidation.ts` parse responses and synchronize caches.                                                                                                        | Add typed definition/draft/correction calls. Invalidate exact template/schedule/session/history keys after success; on 409/network failure roll back cache only and keep visible draft.                                                                             |
| Feedback projection/audit UI               | `apps/api/src/middleware/feedback-note-projection.ts` projects response notes in-place; `apps/web/src/features/workouts/components/{feedback-audit,exercise-detail-modal}.tsx` consume private audit/exercise data.                                                                            | Prove projection does not rewrite question definitions, answer notes/context, or revision history. Keep private text out of generic responses/enrichment.                                                                                                           |
| Agent transforms/enrichment                | `apps/api/src/middleware/{auth,agent-request-transform,agent-enrichment}.ts` unifies JWT/AgentToken ownership and mutation enrichment.                                                                                                                                                         | Preserve identical owner boundaries and explicit actor/source metadata. Do not summarize raw question/answer text into generic enrichment or logs.                                                                                                                  |
| Exercise/template deletion and trash       | `routes/exercises/index.ts`, `routes/trash/index.ts`, and scheduled/session snapshots allow live sources to be removed or swapped.                                                                                                                                                             | Frozen exercise IDs/names/body context survive deletion/swap/template deletion. Cross-owner exercise and concern snapshots fail closed on writes.                                                                                                                   |
| Progression                                | `routes/workout-progression/store.ts` reads only source-linked actionable native feedback.                                                                                                                                                                                                     | Regression-test that dynamic values cannot bypass #149 classification or become automatic scores/clearance. No #151 interpretation.                                                                                                                                 |
| Adaptive nutrition                         | `routes/adaptive-nutrition/review-store.ts` parses session feedback under #149 semantics.                                                                                                                                                                                                      | Preserve exact exclusions; dynamic questions never become recovery/readiness inputs automatically.                                                                                                                                                                  |
| Agent context/dashboard/habits             | `routes/agent/context-store.ts`, `routes/v1/context.ts`, dashboard stores, and habit resolvers read session identity/status rather than dynamic feedback.                                                                                                                                      | Keep existing behavior. Rich feedback retrieval/search/deduplication, recurrence/suppression, and concern disposition remain #151.                                                                                                                                  |
| Migration/schema registry                  | `apps/api/drizzle/meta/_journal.json` ends at `0061_feedback_provenance_audit`; `apps/api/src/db/schema/index.ts` exports current tables; migration and backfill tests install staged/current schemas.                                                                                         | Add a forward `0062` migration and schema exports. Build an exact through-0061 predecessor, apply only 0062, separately install fresh current, prove rollback/idempotence/restore on synthetic copies, and never alter 0061.                                        |
| Broad compatibility fixtures               | `apps/api/src/__tests__/workouts.test.ts`, workout route/store tests, `apps/web/src/App.test.tsx`, `rir-history-fixture.ts`, and provenance/scheduling Playwright suites encode legacy clients and shapes.                                                                                     | Preserve old-client and legacy schema-v2/older reads; extend expectations only where new optional response fields appear. Do not weaken historical assertions.                                                                                                      |
| Browser acceptance                         | `apps/web/e2e/{workout-session,workout-scheduling,scheduled-provenance,feedback-provenance}.spec.ts` and `playwright.feedback-synthetic.config.ts` provide existing synthetic browser patterns.                                                                                                | Add a populated fictional #150 flow with raw API/DB readback, both start surfaces, draft/reload/failure, completion/correction/history, removed source, tib-bar exclusivity/non-recurrence, 375px/desktop/keyboard/accessibility/console/network/overflow evidence. |

## Explicit #151 deferrals

- No longitudinal feedback context/search/deduplication API.
- No semantic recurrence, suppression, conflict/exposure interpretation, or concern resolution.
- No notification, scheduler, or automatic next-morning response.
- No future programming-note rewrite.
- No diagnosis, medical threshold, readiness/recovery/technique derivation, or clearance.

## Implementation order

1. Add shared contracts and hostile schema tests.
2. Add normalized immutable definition/list/answer revision tables and forward migration proof.
3. Wire owner-scoped template, scheduled snapshot/override, and session freeze/draft/correction stores and routes.
4. Reuse `SessionFeedback` with frozen definitions and server/local revision-aware hydration; update history/caches.
5. Add focused API/auth/compatibility/UI/E2E tests and synthetic evidence capture.
6. Run Luna medium adversarial review, fix every in-scope finding, then run fresh uncached focused/full gates and built-in-browser acceptance from the final commit.
