pnpm lint-staged
pnpm turbo typecheck
# Run package test tasks serially — vitest already parallelizes inside each
# package. Running api + web test pools concurrently oversubscribes CPU and
# makes testing-library waitFor/findBy calls flake at their 1s timeout.
pnpm turbo test --concurrency=1
